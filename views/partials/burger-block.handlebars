<li>
	{{burger_name}}

	<button class="change-devoured btn btn-warning" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Make another!{{else}}DEVOUR!{{/if}}
	</button>

	<button class="delete-burger btn btn-danger" data-id="{{this.id}}">DELETE!</button>
</li>